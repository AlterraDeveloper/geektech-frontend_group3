export default class Task {
  constructor(text, duration, priority) {
    this.text = text;
    this.priority = priority;
    this.duration = duration;
  }
}

// function Task(text, duration, priority) {
//   this.text = text;
//   this.priority = priority;
//   this.duration = duration;
// }
